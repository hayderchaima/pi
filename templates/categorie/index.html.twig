{% extends 'admin.html.twig' %}
{% block main  %}
    <div class="card-body">
        <h5 class="card-title">s</h5>

        <!-- Default Table -->
        <table class="table">
            <thead>
            <tr>

                <th scope="col">nom</th>
                
                <th scope="col">color</th>
                
                
                <th scope="col">Actions</th>
            </tr>
            </thead>
            <tbody>
            <tr {% for a in Categorie %}>

                <td>{{ a.name }}</td>
                
                <td style="color: {{ a.color }}">{{ a.color }}</td>

                
                <td> <a class="btn cur-p btn-danger" href="{{ path('delete_rec',{'id':a.id}) }}"> Delete</a> <a class="btn cur-p btn-success" href="{{ path('update_rec',{'id':a.id}) }}"> update</a> </td>

            </tr {% endfor %} >

            </tbody>
        </table>
        <div class="pagination">
            {{ knp_pagination_render(Categorie) }}
        </div>
        <!-- End Default Table Example -->
    </div>
{% endblock %}